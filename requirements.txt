# core (conda-forge or PyPI; ngmix and rustfits are conda-forge)
numpy
scipy
matplotlib
sep
ngmix
rustfits
# the Gaia tools (lsst-starsub-make-gaia, the visit Gaia files)
astropy
# The LSST science pipelines are required for the butler loaders
# (lsst_starsub.visit.exposure, lsst_starsub.coadd.cellcoadd) and the
# command line; the rest of the package, and the tests, work without
# them.  Nothing is required from lsst_mdet, which calls this package
